# SDD ledger — plan: /Users/laccd/code/calricula/docs/applicationx/plans/2026-09-18-auth-logto-calricula-migration.md

Repo: /Users/laccd/code/calricula, branch `auth/logto-migration` created from `codex/calricula-pwa-demo-release` HEAD f5b4020 (main is 62 commits behind that branch; PR #35 stacks the demo branch onto main). Working in place (no worktree) so the untracked planning files and frontend node_modules stay available. Trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` (CLAUDE.md names Opus 4.8; harness reminder overrides to Fable 5.1, consistent with the applicationx repo).
Spec: ADR-0001; EMBEDDED-INTERFACE §4; CLAUDE.md constraints (coverage floor 45%, WCAG, light-only, BSD-3 branding, no personal emails).
Environment (2026-09-19): backend venv being created at backend/venv (Python 3.12); host port 5433 is a Langfuse Postgres, so tests use a disposable container `calricula-test-db` on 5435 via DATABASE_URL; frontend node_modules present; Playwright config present.

## Pre-flight scan
| Pair / task | Produces vs consumes | Finding |
| --- | --- | --- |
| T1→T2 | oidc.verify_access_token/verify_id_token/resolve_dev_token ↔ deps.get_current_user, routes/auth.login | Consistent as planned. |
| T2 self | column rename firebase_uid→auth_subject; seeds/tests reference firebase_uid (7 test files, conftest fixtures, seeds) | Large mechanical rename; T2 owns all of it. |
| T2 self | `/login` takes the ID token while every other route takes the access token | Two verifiers, two audiences (client id vs API resource). Documented. |
| T2 self | DEMO_MODE reads email from the ID token; other routes never see email | Demo gate only at /login; get_current_user must not re-check demo email. **Ruling below.** |
| T3 self | AuthContext.test mocks '@/lib/firebase'; useUserCourses imports getIdToken from lib/firebase | T3 rewrites both. |
| T3 self | jest coverage thresholds are 5–6% globally | Not a gate risk. |
| T3→host plan | `/api/auth/token?resource=` for the ApplicationX broker (T5) | The brokered adapter is the host plan's; T5 delivers the route + config only. **Ruling below.** |
| T4 | docker-compose files pass FIREBASE env through | Replace with OIDC/LOGTO vars. |
| All | CI backend job DB setup | Check whether CI names FIREBASE env; adjust in T4. |

## Rulings (pre-flight)
- Ruling: base branch is the demo branch, not main (main is 62 commits behind; the auth code lives on the demo line). Cost if wrong: rebase.
- Ruling: tests run against a disposable Postgres on 5435 via `DATABASE_URL`; CI keeps its own service. Cost if wrong: none (env only).
- Ruling: DEMO_MODE is enforced once, at `/api/auth/login` (ID token email must contain "demo"); protected routes rely on the user row created there. A demo user who never called /login has no row → get_current_user auto-provisions FACULTY today; in DEMO_MODE auto-provision is refused (403) so the demo gate cannot be bypassed by skipping /login. Cost if wrong: one guard.
- Ruling: T5 ships the per-resource token route and config only; the brokered adapter stays in the host plan. Cost if wrong: none.
Baseline (before any change, DB 5435 seeded with CI's recipe): 235 passed, 1 failed — test_security_regressions::test_protected_endpoint_rejects_forged_token returns 200 locally because the repo-root `.env` sets AUTH_DEV_MODE and Firebase init fails (CI has no .env and passes). The OIDC verifier must make this pass regardless (forged JWT → 401). Coverage 50.36%.
Ruling: Task 1 adds PyJWT and OIDC settings/guards but keeps firebase-admin and FIREBASE_* until Task 2 deletes firebase.py (avoid a broken intermediate commit). Cost if wrong: none.
Task 1: implementer ae6ef7549d11c6438, commit 62b3e5e, DONE (23 new tests; two ws4 hygiene tests updated to supply OIDC settings for the production happy path; forged-token baseline failure unchanged until Task 2).
Task 1: complete (commits f5b4020..62b3e5e, review clean; 1 minor: _jwks_url called twice per verification)
Ruling: `alembic upgrade head` on a fresh DB (5435/calricula_alembic) is clean, but `alembic check` reports extensive pre-existing model/migration drift (indexes, nullability, JSON vs JSONB, notification enums) that predates this work. Gate for Task 2: the new migration applies on head, downgrades, and re-applies; `alembic check` is not a gate and the drift is recorded as a separate follow-up. Cost if wrong: none for auth.
Task 2: implementer a073e43676a34c80f (opus), commit 322971b, DONE with concerns: (1) users.email is NOT NULL so access-token provisioning stores `{sub}@oidc.invalid` until /login supplies a verified address; (2) firebase grep hits are only the deprecated `firebase_uid` alias (on UserResponse and UserProfileResponse); (3) /check returns `sub` not `uid` (Task 3 frontend must follow); (4) trailer stays Fable 5.1 (owner's convention); (5) test_auth_characterization.py deleted, properties claimed covered by test_oidc.py; resolve_dev_token now returns iss "dev".
Task 2: review (opus): Needs fixes — 2 Important (provisioning_email stores an unverified `email` claim; placeholder-email rule has zero tests), 1 Important design follow-up (legacy re-link window never closes and carries roles), 6 Minor. No takeover path found; deleted characterization test fully covered; /check shape change inert (no frontend caller).
Task 2: Ruling: fix both Importants; fold in `AUTH_LEGACY_RELINK: bool = True` gating the re-link branch (deployers turn it off after cutover; Task 4 documents it and the verified-email-connector requirement), Minor 1 (DEMO_MODE also refuses existing non-demo accounts on protected routes), Minor 3 (repair "New User" full_name from a verified ID-token `name`), Minor 7 (production guard refuses OIDC_AUDIENCE == OIDC_CLIENT_ID). Cost if wrong: small, all tested.
Task 2: minor (deferred): demo gate reads the unverified email claim (moot once email requires verification); duplicate UserProfileResponse classes; concurrent first-contact race → 500 (pre-existing shape).
Task 2: fix round 1/5 (6 addressed, 0 open; commits 322971b..af9626d)
Task 2: minor (deferred): `email_verified is True` strict (string "true" from a future provider → placeholder); audience==client-id guard is production-only.
Task 2: complete (commits 62b3e5e..af9626d, review clean)
Task 3: implementer ae808e9e474c0f540 (opus), commit 5348760, DONE (no getIdToken in @logto/next 4.2.11 → LogtoClient(config).createNodeClient().getIdToken() server-side; tsc has pre-existing repo errors outside touched files; Playwright not run).
Task 3: review (opus): Approved contingent on 1 Important (fetchTokenRef initialised to a stub so child mount effects that call getToken() run unauthenticated once and never retry — e.g. /lmi-data useUserCourses), 8 Minor. Security requirements verified (ID token never in browser; no storage/URL tokens; no open redirect; fail-closed 404; Firebase gone).
Task 3: Ruling: fix the stub race (module-level/useCallback fetchToken; getToken re-fetches when the cached token is missing or within the refresh margin — folds Minor 8); fold in Minor 2 (fake-timer refresh test), Minor 3 (AbortSignal.timeout(5000) on the server-to-server exchange), Minor 6 (map 502 profile_unavailable to a user-visible message). Cost if wrong: small.
Task 3: minor (deferred): GET sign-out (SDK shape; logout CSRF nuisance); unhandled callback errors → 500; "works without JS" comment inaccurate due to the mount gate; getLogtoContext ignoreCookieChange nuance.
Task 3: fix round 1/5 (4 addressed, 0 open; commits 5348760..e70f16f)
Task 3: minor (deferred): logout() does not cancel an in-flight token fetch (masked by the full-page /sign-out navigation); no route-level test for the AbortSignal timeout path.
Task 3: complete (commits af9626d..e70f16f, review clean)
Task 4: implementer a8b75683507608d4f, commit d829b40, DONE (docker-compose.demo.yml has a pre-existing YAML parse bug; calricula_pwa_demo/ left untouched per its boundary rule — reviewer to check whether it depends on Firebase at runtime).
Task 4: review: Needs fixes — 1 Critical: the serviceAccountKey.json-family ignore patterns were removed (unrequested), leaving an existing 0-byte `backend/serviceAccountKey.json` placeholder committable; everything else accurate and cross-checked.
Task 4: Ruling: restore the four credential-file ignore patterns with a comment that they guard deployer leftovers regardless of provider; never delete the untracked placeholder (not ours). Cost if wrong: none.
Task 4: fix round 1/5 (1 addressed, 0 open — ignore patterns restored; commits d829b40..4332e44)
Task 4: complete (commits e70f16f..4332e44, review clean)
Task 5: Ruling: `getToken(resource?: 'calricula' | 'applicationx')` on the AuthContext (default calricula; tokens cached per resource with their own expiry); the token route accepts `?resource=` limited to the two configured indicators (400 otherwise; 204 signed out); the brokered adapter itself stays in the host plan. Cost if wrong: an optional parameter.
Task 5: implementer a1a94bdc3cd5eb4b1, commit bd8b533, DONE (dev mode returns the same id for both resources; route validates resource before auth so unknown/unconfigured → 400/404 even signed out — order is fine, no token leaves).
Task 5: review: Approved, 0 Important, 1 Minor (resource validated before auth → 400/404 even when signed out; no leak).
Task 5: complete (commits 4332e44..bd8b533, review clean)
Task 6 (exit): backend 284 passed / 51.55%; migration up/down/up clean; frontend lint 0 errors, jest 357, build ok; residual Firebase mentions are the deprecated alias, one config comment and history lines; host plan patched (auth_subject, getToken('applicationx')); 8/8 trailers.
E2E (dev mode, live stack, chromium): dev login, token issuance and /api/auth/me verified working. Failures are unrelated to auth and pre-existing on the demo branch: courses page calls `units.toFixed(1)` on a string-serialised Decimal (6 of 8 failures), a hardcoded localhost:3000 URL in login-dashboard-exploration.spec.ts, and clearCookies() not clearing the dev-mode sessionStorage session. Recorded for the owner; not fixed here (outside the migration's scope). Report: e2e-report.md.
Final review (opus, final-review.md): 0 Critical, 4 Important (I-1 seeds NULL issuer adoptable by re-link; I-2 first-sign-in race orphans legacy row; I-3 tracked files cite untracked ADR; I-4 Logto runtime path never executed end-to-end), 13 Minor. All deferred minors triaged: stay deferred.
Ruling: fix wave = I-1, I-2 (frontend bootstrap gate + backend placeholder-merge guard with FK-safe fallback), I-4 as a staging/release checklist gate, plus doc/one-line minors M-3, M-4, M-5, M-6, M-7, M-8, M-9 and recommendation 5 (§6 cutover SQL). M-1, M-2, M-10, M-11, M-13 stay deferred (follow-ups). Cost if wrong: small, all tested or docs.
Ruling: I-3 — do NOT commit the ADR: it belongs to the untracked ApplicationX planning set (private companion repo) and publishing any of it is the owner's call; instead fold the decision summary into docs/AUTH-LOGTO.md and reduce tracked references to a bare "ADR-0001" label / a pointer to docs/AUTH-LOGTO.md. Cost if wrong: owner commits the ADR and re-adds links (trivial).
Ruling: M-12 trailer stays `Claude Fable 5.1` for this branch (owner convention across both repos, recorded at Task 2). Cost if wrong: none.
Ruling: plan defects flagged by the reviewer (issuer default for existing rows → NULL; email=None on a NOT NULL column → placeholder) — amend the plan text to match the shipped behaviour. Cost if wrong: none.
Final fix wave: implementer a231e515901206ec7 (opus), commits bd8b533..9f7d467 (b5b39e9 backend, 8fcfa70 frontend, 7717e1f docs, 9f7d467 docstring). Backend 289 passed / 51.68 %; jest 361; lint/build green.
Final re-review (opus, final-rereview.md): 11/11 addressed. New: Important N-1 (README/prod-compose seed steps now omit seed_departments → prod has no departments, courses cannot be created — introduced by the implementer's extra edit); Minor N-2 (relink docstring overstates FK refusal: notifications cascade), N-3 (merge drops admin edits on a placeholder silently), N-4 (already-seeded DBs keep NULL-issuer seed rows; §6 lacks remediation SQL), N-5 (stale note RELEASE_CHECKLIST.md:69).
Ruling: N-1/N-4/N-5 are doc-only gaps introduced by the fix wave itself; resume the same implementer for a docs-only addendum and verify the diff directly (controller read) instead of a third review seat — the skill's "no second fix wave" guards against loops, not against correcting a few doc lines the wave broke. N-2 (soften docstring) folded in. N-3 stays deferred. Cost if wrong: doc lines visible in the PR.
Plan text amended (untracked plan file): Global Constraint issuer default → NULL for existing rows; Task 2 email=None → placeholder. Both marked 'amended after execution'.
Final fix addendum: commit 17e4805 (docs-only + docstring; N-1, N-2, N-4, N-5). Controller verified the diff directly (5 files, 28+/9-): seed_departments added to prod steps; `test\_%` matches seed subjects; no logic change.
Deferred (follow-ups, not blocking): M-1 duplicate UserProfileResponse; M-2 _jwks_url twice; M-10 useCallback on getToken/login/logout; M-11 docker-compose.demo.yml YAML parse error (pre-existing); M-13 no route test for GET /api/auth/token; N-3 placeholder merge drops admin edits silently; alembic check drift (pre-existing); three pre-existing e2e failures (e2e-report.md).
Final review: complete (commits f5b4020..17e4805, 13 commits; whole-branch review + one fix wave + scoped re-review + docs addendum; 0 open Critical/Important).
