# SDD ledger — plan: docs/applicationx/plans/2026-09-19-workspace-ui-github-packages-and-task8.md
Owner decisions 2026-09-19: merge stack (#35, #36, #37 merged to main, e5729a6); publish workspace-ui to GitHub Packages (private); gh token refreshed with write:packages.
Pre-flight: Task A (applicationx repo, base b719a4a) → Task B (calricula, base main e5729a6). Seam: package name `@johnnyrobot/workspace-ui` (scope must equal owner) — Task B imports use the new name; host plan Task 8 text says `@applicationx/workspace-ui` → superseded. CSS import inside the package index (`import './theme/tokens.css'`) is a global stylesheet import from node_modules: allowed in Next App Router — Task B must verify the build. Ruling: `@applicationx` remains the CSS/theme prefix (`--ax-*`), only the npm scope changes. Cost if wrong: a rename.
Task A: implementer aa061239ef639c356 (sonnet), commit 5107a77 on applicationx main; review (sonnet) Approved, 2 pre-existing minors (lockfile root version drift 0.0.1; frontend lock hand-edited). Tag workspace-ui-v0.1.0 pushed; publish run 35462687483 succeeded; `@johnnyrobot/workspace-ui@0.1.0` private, linked to johnnyrobot/applicationx.
Task A: complete (commit b719a4a..5107a77, review clean)
Task B: branch feat/workspace-ui-shell off main e5729a6. Open item for the owner: Calricula CI needs read access to the package (package "Manage Actions access" → add johnnyrobot/calricula, or a fine-grained PAT with read:packages as repo secret WORKSPACE_UI_READ_TOKEN).
Task B: implementer ab4a4f629e82047aa (opus), commits fd882b7, d6f2b6f; jest 443; build; lint 0 errors; pytest 331; live Playwright 7 passed + 1 skipped, STUB_DOWN 1 passed; Docker deps stage built with the secret.
Final review (opus, final-review.md): 0 Critical, 2 Important (I-1 docs/CI claim GITHUB_TOKEN can read the package — false until the owner grants the calricula repo Actions access or sets WORKSPACE_UI_READ_TOKEN; I-2 shell renders a focusable "Open in ApplicationX" that no-ops when APPLICATIONX_STANDALONE_URL is unset — package can't hide it), 7 Minor.
Ruling: fix wave = package 0.1.1 in applicationx (sideEffects "**/*.css"; ContextHeader hides "Open in ApplicationX" when no onOpenStandalone handler is given) → tag/publish → Calricula bumps the pin, drops the explicit tokens.css workaround, passes onOpenStandalone only when a standalone URL exists, rewrites the CI/docs wording for I-1, and fixes the adapter-memo comment. Cost if wrong: one more patch release.
Package 0.1.1: commit a103c11 on applicationx main (controller-verified diff: sideEffects glob, showOpenStandalone prop, tests, CHANGELOG); tag workspace-ui-v0.1.1 published.
Task B: fix round 1/5 (4 addressed, 0 open — pin 0.1.1, single standalone control, access-step docs, memo comment; commit d6f2b6f..e82d8df). jest 444; build (.ax-shell in built CSS); lint 0 errors.
Task B: minor (deferred): explicit `@johnnyrobot/workspace-ui/tokens.css` import kept — the package's own CSS import still yields no `.ax-*` rules under `next build` even with 0.1.1 (documented in the page comment; investigate in the package: CSS import inside dist/index.js vs Next's node_modules CSS handling); adapter memoised on getToken (benign); ready page resolves the context twice (page + shell).
Task B: complete (commits e5729a6..e82d8df, review clean)
Final review: complete. Owner action required before CI can pass: grant johnnyrobot/calricula access to the package (Manage Actions access) or set WORKSPACE_UI_READ_TOKEN.
