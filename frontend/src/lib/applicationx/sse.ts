/**
 * The SSE parser is the shared package's (`@johnnyrobot/workspace-ui`); the
 * local port that lived here until Task 8 was replaced by this re-export.
 */
export { subscribeSSE, type SubscribeSSEOptions } from '@johnnyrobot/workspace-ui';
